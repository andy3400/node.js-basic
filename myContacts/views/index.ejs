<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>연락처 관리하기</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="css/style.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" integrity="sha512-iecdLmaskl7CVkqkXNQ/ZH/XLlvWZOJyj7Yy7tcenmpD1ypASozpmT/E0iPtmFIB46ZmdtAc9eNBvH0H/ZpiBw==" crossorigin="anonymous" referrerpolicy="no-referrer" />
</head>
<body>
  <%- include("./include/_header") %>
  
  <!-- Main Content -->
  <main id="site-main" class="container mt-4">
    <div class="d-flex justify-content-between mb-4">
      <a href="/contacts/add" class="btn btn-light">
        <i class="fa-solid fa-user-plus"></i> 연락처 추가
      </a>
      <h2>연락처 목록</h2>
    </div>
  
    <table class="table table-striped">
      <thead>
        <tr>
          <th>이름</th>
          <th>메일 주소</th>
          <th>전화번호</th>
          <th>&nbsp;</th>
        </tr>
      </thead>
      <tbody>
        <% contacts.forEach(contact => { %> <!-- 'contacts'에서 'contact'로 바꿈 -->
        <tr>
          <td><%= contact.name %></td> <!-- 'contact.name'으로 수정 -->
          <td><%= contact.email %></td> <!-- 'contact.email'으로 수정 -->
          <td><%= contact.phone %></td> <!-- 'contact.phone'으로 수정 -->
          <td>
            <a href="/contacts/<%= contact._id %>" class="btn btn-primary btn-sm" title="수정">
              <i class="fas fa-pencil-alt"></i>
            </a>
            <a href="#" class="btn btn-danger btn-sm" title="삭제">
              <i class="fas fa-times"></i>
            </a>                
          </td>
        </tr>
        <% }) %>
      </tbody>
    </table>
  </main>
  <!-- /Main Content -->
  
  <%- include("./include/_footer") %>

</body>
</html>